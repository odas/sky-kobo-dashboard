# Secrets — never commit
.env

# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Downloaded audiobooks (193MB+). The fetcher that made these, get_mp3.py,
# moved to ~/dev/noetone-batch/scripts/ on 2026-08-30 — the audio stayed here
# because it is re-fetchable, not because it belongs here.
Asimov_Downloads/

# Hoarded old copies, kept locally out of version control
_archive/

# ── docs/ is LOCAL-ONLY, fleet-wide convention (2026-08-08) ────────────────
# Nothing under docs/ goes to GitHub. STATUS.md and fixes.md hold in-progress
# decisions and personal notes a public reader shouldn't see, and one shape
# across every app beats per-repo exceptions.
# History lives in a nested repo at docs/.git which has NO REMOTE — there is
# no push path to GitHub at all, by construction rather than by discipline.
#
# The whole DIRECTORY is excluded, deliberately. The `docs/*` form (which
# exists so `!docs/keep-me.md` re-includes can work) is WRONG here: git stops
# at the nested docs/.git and reports the repo itself as `?? docs/`, leaving
# every git status noisy and one `git add -A` away from committing docs/ as a
# stray gitlink. No re-includes are wanted, so exclude the folder outright.
docs/
