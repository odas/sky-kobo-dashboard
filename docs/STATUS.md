# kobo-sky — status report

Last updated: **2026-07-08** (update this date every session that changes anything)

## Current state

Kobo e-ink dashboard (star map, weather, IST). On GitHub as
`odas/sky-kobo-dashboard`. Local WIP (1-line app.py change) committed
2026-07-08.

## Done 2026-07-08: repo cleanup

Added `.gitignore` (was none); untracked `.DS_Store`; started tracking
`get_mp3.py` (Internet Archive audiobook fetcher — feeds the Noëtone
listening pipeline); `Asimov_Downloads/` (193MB of fetched audio) stays
local, gitignored.
