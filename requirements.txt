# Pinned to what the local .venv actually runs (verified 2026-08-30).
# Nothing deploys from this file — the app runs from the working tree under
# pm2. The pins exist so a fresh clone reproduces a known-good app.
Flask==3.1.3
requests==2.34.2
ephem==4.2.1
gunicorn==26.0.0
