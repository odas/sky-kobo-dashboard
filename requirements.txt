# Pinned to what the local .venv actually runs (verified 2026-08-30).
# Render installs from this file, so drift here means local and deployed
# are different apps.
Flask==3.1.3
requests==2.34.2
ephem==4.2.1
gunicorn==26.0.0

# get_mp3.py only — the Internet Archive audiobook fetcher that feeds
# Noëtone. Not imported by app.py; Render installs it harmlessly.
internetarchive==5.8.0
